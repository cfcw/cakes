<div class="navbar navbar-default navbar-fixed-top">
	<div class="container">
		<div class="navbar-header">
			<% if session[:is_login] %>
			<%= link_to("Cakes for Co-Workers", mypage_main_path, {"style" => "font-family:Futura;", "class" => "navbar-brand"}) %>
			<% else %>
			<%= link_to("Cakes for Co-Workers", root_path, {"style" => "font-family:Futura;", "class" => "navbar-brand"}) %>
			<% end %>

			<button class="navbar-toggle" type="button" data-toggle="collapse" data-target="#navbar-main">
				<span class="icon-bar"></span>
				<span class="icon-bar"></span>
				<span class="icon-bar"></span>
			</button>
		</div>
		<div class="navbar-collapse collapse" id="navbar-main">
			<ul class="nav navbar-nav navbar-right">
				<li>
					<%= link_to("ご利用ガイド", guide_index_path, {:target => "_blank", :width => "200", :height => "200"}) %>
				</li>
				<li>
					<% if session[:is_login] %>
					<%= link_to("ログアウト", authentication_logout_path) %>
					<% else %>
					<%= link_to("ログイン", login_index_path) %>
					<% end %>
				</li>
			</ul>
		</div>
	</div>
</div>
