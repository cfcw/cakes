<%= form_for(@target_age) do |f| %>
  <% if @target_age.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@target_age.errors.count, "error") %> prohibited this target_age from being saved:</h2>

      <ul>
      <% @target_age.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :target_age_range %><br />
    <%= f.text_field :target_age_range %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
